# Key changes:
# 1. Capture output to variable: OUTPUT=$(oss-trust check-all ...)
# 2. Export to GitHub output: echo "JSON_OUTPUT<<EOF"
# 3. GitHub script reads from output, not file: ${{ steps.check.outputs.JSON_OUTPUT }}
# 4. Post JSON directly to comment
