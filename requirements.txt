# requirements.txt
# Direct runtime dependencies for projects using the OSS Trust Framework.
# Pin to exact versions in production; use >= here as a starter template.
# Run: python check_all.py   to validate all packages below against the pipeline.

httpx==0.27.0
pyyaml==6.0.2
python-dotenv==1.2.2
click==8.1.7
pydantic==2.7.4
pyotp==2.9.0
cryptography==46.0.7
rich==13.7.1
